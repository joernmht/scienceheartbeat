<!-- Thanks for contributing to scienceheartbeat. -->

## Summary

<!-- One or two sentences describing the change. -->

## Why

<!-- Link to issue or describe the motivation. -->

## Test plan

- [ ] `pytest` passes
- [ ] `ruff check src tests examples` passes
- [ ] `ruff format --check src tests examples` passes
- [ ] `mypy src/scienceheartbeat` passes
- [ ] If `app.js` changed: `node --check` passes
- [ ] If a versioned resource or the schema changed: version bumped + CHANGELOG note
- [ ] The emitted `heartbeat.json` is still deterministic for identical inputs

## Notes

<!-- Anything reviewers should pay particular attention to. -->
